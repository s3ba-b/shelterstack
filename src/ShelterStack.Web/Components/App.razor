@using System.Globalization
<!DOCTYPE html>
<html lang="@CultureInfo.CurrentUICulture.TwoLetterISOLanguageName">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <ResourcePreloader />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
    @* app.css is generated from Styles/app.tailwind.css by the Tailwind CLI on build. The
       Blazor scoped-CSS bundle is gone — this project no longer ships any .razor.css. *@
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <ImportMap />
    <HeadOutlet @rendermode="InteractiveServer" />
</head>

<body>
    <Routes @rendermode="InteractiveServer" />
    <ReconnectModal />
    <script src="@Assets["js/language.js"]"></script>
    <script src="@Assets["_framework/blazor.web.js"]"></script>
</body>

</html>

@* Interactive server with prerendering off: the authenticated session lives in the circuit, so
   evaluating auth state (and reading the token) only makes sense once the circuit is live —
   prerendering would first render every protected route as signed-out. *@
@code {
    private static readonly InteractiveServerRenderMode InteractiveServer = new(prerender: false);
}
